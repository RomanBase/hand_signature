name: hand_signature
description: The signature pad widget that allows to draw signature with finger.
version: 0.5.0

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  vector_math: ^2.0.8

  flutter_svg: ^0.17.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
